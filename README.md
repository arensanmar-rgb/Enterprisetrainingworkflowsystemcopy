
  # Enterprise Training Workflow System (Copy) (Copy)

  This is a code bundle for Enterprise Training Workflow System (Copy) (Copy). The original project is available at https://www.figma.com/design/Q1K0xRrC5liGh2VPJ0TQNx/Enterprise-Training-Workflow-System--Copy---Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  